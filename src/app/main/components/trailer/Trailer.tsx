import { Container, Heading, StyledSurfIcon } from './Trailer.styles';

export const Trailer = () => {
  return (
    <Container>
      <Heading>Bez ograniczeń, bez limitów duża baza zamówień publicznych</Heading>
      <StyledSurfIcon />
    </Container>
  );
};
