import { Container, Heading, Box } from './Header.styles';

export const Header = () => {
  return (
    <Container>
      <Box>
        <Heading>Nowa generacja zamówień publicznych</Heading>
      </Box>
    </Container>
  );
};
